# Azure App Service Configuration
# This file tells Azure how to run your FastAPI application

# Python version
PYTHON_VERSION=3.9

# Startup command
STARTUP_COMMAND=uvicorn app.main:app --host 0.0.0.0 --port 8000

# Environment variables (will be set in Azure portal)
# DATABASE_URL will be set to Azure SQL connection string
# SECRET_KEY will be set for production
